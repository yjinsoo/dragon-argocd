apiVersion: v1
kind: ConfigMap
metadata:
  name: python-config
data:
  # property-like keys; each key maps to a simple value
  SQLALCHEMY_DATABASE_URL: "sqlite:////db/my_user_db.db"
  ALGORITHM: "HS256"
  ACCESS_TOKEN_EXPIRE_MINUTES: "30" # 토큰 유효 시간 30분
